IF(WITH_ASSIMP)
    TARGET_LINK_DIRECTORIES(${targetname} PUBLIC ${ASSIMP_LIBRARY_DIRS})
    target_link_libraries(${targetname} PUBLIC ${ASSIMP_LIBRARIES} -ldl)
    TARGET_COMPILE_DEFINITIONS(${targetname} PUBLIC COMPILE_WITH_ASSIMP)
ENDIF()